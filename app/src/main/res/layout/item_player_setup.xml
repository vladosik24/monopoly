<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:orientation="horizontal" android:gravity="center_vertical"
    android:padding="8dp" android:layout_marginBottom="12dp">

    <TextView android:id="@+id/tvAvatar"
        android:text="🚀" android:textSize="28sp" android:gravity="center"
        android:layout_width="48dp" android:layout_height="48dp"
        android:background="@drawable/circle" android:padding="4dp"
        android:backgroundTint="#22FFFFFF"/>

    <EditText android:id="@+id/etPlayerName"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_weight="1" android:layout_marginStart="12dp"
        android:textColor="@color/text" android:textSize="15sp"
        android:backgroundTint="@color/border"
        android:inputType="textPersonName"/>

    <View android:id="@+id/vColor"
        android:layout_width="16dp" android:layout_height="16dp"
        android:layout_marginStart="12dp"
        android:background="@drawable/circle"/>
</LinearLayout>
